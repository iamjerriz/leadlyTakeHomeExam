import { createApp } from '../src/app';

// Vercel serverless entry point. No app.listen() here - Vercel manages the
// request/response lifecycle and invokes this handler per request.
export default createApp();
